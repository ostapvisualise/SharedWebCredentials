    import XCTest
    @testable import SharedWebCredentials

    final class SharedWebCredentialsTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SharedWebCredentials().text, "Hello, World!")
        }
    }
